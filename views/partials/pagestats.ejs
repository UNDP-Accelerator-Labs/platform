<%
    const originalUrl = locals.metadata.page.originalUrl;
    const readTime = 6000;  // time it takes to register a read in milliseconds
    const pagestatsbase = {
        url: originalUrl,
        readtime: readTime
    }
%>
<data name='pagestats' value='<%= JSON.stringify(pagestatsbase) %>'></data>
<script type='module' src='/js/pagestats.js'></script>
